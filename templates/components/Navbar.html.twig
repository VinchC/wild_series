<div{{attributes}}>
	<div class="dropdown">
		<button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
			Catégories
		</button>
		<ul class="dropdown-menu">
			{% for category in this.categories %}
				<li>
					<a class="dropdown-item" href="{{ path('app_category_show', {categoryName: category.name}) }}">{{ category.name }}</a>
				</li>
			{% endfor %}
		</ul>
	</div>
</div>
